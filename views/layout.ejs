<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Chettinad - Premium Sarees' %></title><title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Chettinad - Premium Sarees' %></title>
  <!-- Common & Dynamic Styles -->
  <link rel="stylesheet" href="/css/common.css">
  <link rel="stylesheet" href="/css/header.css">
  <% if (typeof pageCSS !== "undefined") { %>
    <link rel="stylesheet" href="/css/<%= pageCSS %>">
  <% } %>

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>

<body>
  <%- include('partials/user/header', { user, currentPath }) %>

  <!-- Dynamic Page Content -->
  <main>
    <%- body %>
  </main>

  <%- include('partials/user/footer', { user }) %>
    <script src="/js/common.js"></script>

    <% if (typeof pageJS !== 'undefined') { %>
      <script src="/js/<%= pageJS %>"></script>
    <% } %>
</body>
</html>
